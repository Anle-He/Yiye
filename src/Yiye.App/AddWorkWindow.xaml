<Window x:Class="Yiye.AddWorkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="添加作品" Height="650" Width="540" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Icon="Assets/app-icon.ico"
        Background="{StaticResource PageBrush}" FontFamily="Segoe UI, Microsoft YaHei UI">
    <Grid Margin="30">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="48"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Width="40" Height="40" CornerRadius="12" Background="{StaticResource AccentSoftBrush}" VerticalAlignment="Top">
                <ui:SymbolIcon Symbol="Library24" FontSize="20" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1">
                <TextBlock Text="LIBRARY / 作品资料" Style="{StaticResource DialogEyebrowStyle}" />
                <TextBlock x:Name="HeadingText" Text="收录作品" Style="{StaticResource DialogTitleStyle}" />
                <TextBlock x:Name="DescriptionText" Text="先建立作品资料，完成后再留下属于你的记录。" Style="{StaticResource DialogDescriptionStyle}" />
            </StackPanel>
        </Grid>
        <ScrollViewer Grid.Row="1" Margin="0,22,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Border x:Name="WorkFormSection" Style="{StaticResource FormSectionStyle}" Margin="0,0,8,0">
              <StackPanel>
                <TextBlock Text="作品类别" Style="{StaticResource FieldLabelStyle}" />
                <ComboBox x:Name="KindBox" SelectedIndex="0" Height="42" SelectionChanged="KindBox_SelectionChanged">
                    <ComboBoxItem Tag="book" Content="书籍"/><ComboBoxItem Tag="screen" Content="影视"/>
                </ComboBox>
                <TextBlock Text="标题 *" Style="{StaticResource FieldLabelStyle}" Margin="0,19,0,7" />
                <ui:TextBox x:Name="TitleBox" MaxLength="200" Height="42" PlaceholderText="作品名称" TextChanged="TitleBox_TextChanged" />
                <TextBlock x:Name="TitleError" Text="请输入标题" Foreground="#A33A32" Margin="2,6,0,0" Visibility="Collapsed" />
                <TextBlock Text="副标题（选填）" Style="{StaticResource FieldLabelStyle}" Margin="0,19,0,7" />
                <ui:TextBox x:Name="SubtitleBox" MaxLength="300" Height="42" PlaceholderText="例如：音乐与黄昏五故事集" />
                <StackPanel x:Name="AuthorPanel">
                    <TextBlock Text="作者（选填）" Style="{StaticResource FieldLabelStyle}" Margin="0,19,0,7" />
                    <ui:TextBox x:Name="AuthorBox" MaxLength="200" Height="42" PlaceholderText="例如：石黑一雄" />
                </StackPanel>
                <Border Margin="0,20,0,0" Padding="13,11" CornerRadius="10" Background="{StaticResource AccentSoftBrush}">
                    <TextBlock Text="这里只保存作品资料，不代表阅读或观看状态。" Foreground="{StaticResource AccentStrongBrush}" TextWrapping="Wrap" />
                </Border>
              </StackPanel>
            </Border>
        </ScrollViewer>
        <Border Grid.Row="2" Style="{StaticResource DialogActionBarStyle}">
            <Grid><TextBlock Text="稍后可在详情页继续补充" Foreground="{StaticResource MutedBrush}" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="取消" Appearance="Secondary" Margin="0,0,10,0" Click="Cancel_Click" />
                <ui:Button x:Name="AddButton" Content="收录作品" Icon="{ui:SymbolIcon Add24}" Appearance="Primary" Click="Save_Click" IsDefault="True" IsEnabled="False" />
            </StackPanel></Grid>
        </Border>
    </Grid>
</Window>
