name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: windows-2025

    steps:
      - name: Check out source
        uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.400

      - name: Restore locked dependencies
        run: dotnet restore .\Yiye.slnx --locked-mode

      - name: Build
        run: dotnet build .\Yiye.slnx --no-restore -c Release

      - name: Test
        run: dotnet test .\Yiye.slnx --no-build -c Release --filter "Category!=Manual&Category!=LocalData"
